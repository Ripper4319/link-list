public class node
{
    public char data;
    public node nextnode;
}

public node(char d)
{
    data=di
}

public void insert(node head, node newnode)
node search = head

    for (;search.nextnode !=null)
    searchnode = searchnode.nextnode;

